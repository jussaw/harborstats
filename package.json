{
  "name": "harborstats",
  "private": true,
  "scripts": {
    "dev": "pnpm -F web dev",
    "build": "pnpm -F web build",
    "start": "pnpm -F web start",
    "lint": "pnpm -F web lint",
    "format": "pnpm -F web format",
    "format:check": "pnpm -F web format:check"
  }
}
